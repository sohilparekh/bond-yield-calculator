# Sync UI Workflow

Description: Generate frontend components and types based on a backend entity.

## Steps

1. Read the specified entity file in `/api/entities/`.
2. Generate a corresponding TypeScript interface in `/web/types/`.
3. Create a basic 'Molecule' component in `/web/molecules/` that displays the entity data.
4. Update the main component registry if necessary.
